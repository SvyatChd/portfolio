const photos=[...document.querySelectorAll('.photo')];
const dialog=document.querySelector('dialog');let current=0;
function show(index){current=(index+photos.length)%photos.length;const img=photos[current].querySelector('img');document.querySelector('#full').src=img.src;document.querySelector('#full').alt=img.alt;document.querySelector('#caption').textContent=photos[current].nextElementSibling.querySelector('h3').textContent;document.querySelector('#counter').textContent=`${String(current+1).padStart(2,'0')} / ${photos.length}`;}
photos.forEach((button,i)=>button.addEventListener('click',()=>{show(i);dialog.showModal()}));
document.querySelector('#close').onclick=()=>dialog.close();document.querySelector('#prev').onclick=()=>show(current-1);document.querySelector('#next').onclick=()=>show(current+1);
dialog.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();show(current-1)}if(e.key==='ArrowRight'){e.preventDefault();show(current+1)}});dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});
let startX=0,startY=0;const full=document.querySelector('#full');full.addEventListener('touchstart',e=>{startX=e.changedTouches[0].screenX;startY=e.changedTouches[0].screenY},{passive:true});full.addEventListener('touchend',e=>{const dx=e.changedTouches[0].screenX-startX,dy=e.changedTouches[0].screenY-startY;if(Math.abs(dx)>65&&Math.abs(dx)>Math.abs(dy))show(current+(dx<0?1:-1))},{passive:true});
if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('reveal');observer.unobserve(entry.target)}})},{threshold:.08});document.querySelectorAll('figure,.about').forEach(el=>observer.observe(el));}
